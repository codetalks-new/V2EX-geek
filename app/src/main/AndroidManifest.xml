<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.banxi1988.v2exgeek" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".demo.DataBindingActivity"
            android:exported="true"
            android:label="@string/title_activity_data_binding" >
        </activity>
        <activity
            android:name=".demo.RecyclerViewBindingDemoActivity"
            android:exported="true"
            android:label="@string/title_activity_recycler_view_binding_demo" >
        </activity>
        <activity
            android:name=".controller.TopicDetailActivity"
            android:label="@string/title_activity_topic_detail" >
        </activity>
    </application>

</manifest>
